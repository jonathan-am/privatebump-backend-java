spring:
    application:
        name: "ShopBra"
    datasource:
        url: "jdbc:mariadb://localhost:3306/Afiliados"
        username: shopbra
        password: root
        driver-class-name: org.mariadb.jdbc.Driver
    cache:
        type: redis
server:
    port: 5555
    
logging:
    level:
        org.springframework: INFO
        br.com.shopbra: INFO